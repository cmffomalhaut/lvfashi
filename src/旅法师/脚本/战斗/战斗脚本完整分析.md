# 旅法师战斗脚本完整分析

## 2026-06-03 当前状态速览

这份文档现在按当前实现更新，目的是让后续新对话先抓住“已经修到哪里、哪里仍危险、应该从哪个文件下手”。如果只读一段，优先读本节。

### 当前主线结论

1. 战斗系统现在是“双轨制”：
   - 经典流程：`session.ts -> resolve.ts -> prompt.ts`，从 `prebattle_snapshot + combatants` 构造单回合 preview。
   - Runtime/profile 流程：`App.vue -> store.ts -> runtime-ai.ts -> runtime-session.ts`，用 battle profile 的字段选择、prompt 模板、API 配置、世界书上下文执行单回合/整场/战利品。
2. UI 正常使用时主要走 Runtime/profile 流程；经典流程仍保留，不能随手删。
3. 第三回合以后 `selected_data` 的正确来源已经修正为：
   - `rawMainState` 原始楼层 `stat_data`
   - 叠加 `battle_session.runtime.accumulated_updates`
   - 再按 battle profile 的 `field_selection.selected_fields` 提取
   - 不能改回 `projectMainStateFromBattleSession()` 的投影态，否则字段路径会丢，第三回合又会只剩一小节。
4. Prompt 不是“只发送 runtime_payload”。`runtime-ai.ts` 最终发给 API 的 messages 是：
   - system：当前 prompt 模板的 `system_prompt`
   - user：`user_prompt + runtime_payload JSON + output_contract_prompt`
   - 设置页“AI 请求层”应看 `lastRuntimePrompt.messages/final_user_message`，只看 payload 会误判提示词没发送。
5. 快速整场模式现在会在同一次请求中要求完成战斗终局和战利品：
   - `turn_mode = full_battle`
   - payload 带 `loot_protocol`、`settlement_mode`
   - `appendFullBattleLootContract()` 会额外追加快速整场战利品契约
   - 返回 schema 需要 `loot_result`、`loot_mvu_updates`、`loot_context`
6. 世界书已经接入 Runtime prompt：
   - 自动导入角色绑定世界书和全局启用世界书
   - 手动选择世界书
   - 手动读取并导入具体条目
   - 运行时由 `serializeImportedWorldbooks()` 注入 `runtime_payload.worldbook_context`
7. API/战斗设置持久化已经改为脚本变量镜像：
   - 读取多个候选 scope，选择最新/最完整配置
   - 同时写入 `battle_frontend_settings` 和 `stat_data.battle_frontend_settings`
   - profile 保存后应立即在下拉菜单可见，不需要整页刷新才出现。
8. 关闭按钮当前实现链路是：
   - `App.vue closeWindow()`
   - `store.close()`
   - 直接调用宿主 `__planeswalkerBattleWindowClose`
   - fallback `postMessage({ type: 'planeswalker:battle:close' })`
   - 相关文件：`界面/战斗浮窗/index.ts` 和 `store.ts`。
9. “刷新当前战斗数据”语义是重新读取当前楼层/绑定楼层的 MVU 数据，不是重新 roll AI 回答。重新 roll 当前回合应该走 `resolveAgain()` / “重新结算”按钮。
10. 当前 UI 改动集中在 `App.vue` 和 `global.css`，这两个文件可能有未提交的用户改动。改文档或脚本时不要顺手覆盖它们。

### 后续最容易踩的坑

- 不要把 `_selectedData` 参数重新当作运行时真实数据使用。store 现在会在请求前调用 `createSelectedRuntimeData(profile)` 重新取当前数据，避免 UI 预览传入旧数据。
- 不要让 AI 的 `selected_data_updates` 直接替换 raw MVU。它只累加到 `runtime.accumulated_updates`，用于下一次 prompt 的 selected_data 叠加。
- 不要把快速整场解析当单回合解析。`BattleRoundResultSchema` 里虽有 full_battle 兼容归一化，但正式快速整场应该走 `BattleFullResultSchema` 和 `createPendingPreviewFromFullBattleResult()`。
- 不要照搬 HTYQ 的全局 window/localStorage 架构。本项目只借鉴其世界书条目选择、浮窗交互和 API 模型列表体验。

## 目录结构

```
src/旅法师/
├── index.yaml                          # 角色卡入口（世界书条目、脚本注册）
├── schema.json                         # JSON Schema（单元、战斗态等完整结构）
├── schema.ts                           # Zod Schema（MainState, BattleSession, PlayerCheck 等）
│
├── 脚本/
│   ├── MVU/
│   │   ├── index.ts                    # 仅一行 import: MagVarUpdate bundle
│   │   └── state-access.ts             # 核心：读/写 canonical state 的事务层
│   │
│   ├── 数据结构/
│   │   └── index.ts                    # 不存在
│   │
│   └── 战斗/
│       ├── index.ts                    # 战斗浮窗脚本入口（iframe 挂载 + 启动按钮管理）
│       ├── session.ts                  # ★ 战斗主控制器 & 状态机核心
│       ├── resolve.ts                  # AI 单回合结算（system prompt + generateRaw + JSON schema）
│       ├── prompt.ts                   # prompt 载荷构造（规则摘要 + MVU 数据 + 回合历史 + 上下文）
│       ├── commit.ts                   # 战斗终局写回主状态
│       ├── snapshot.ts                 # 战前快照 + 放弃回滚
│       ├── runtime-ai.ts               # 基于 battle profile 的 AI 请求层（单回合/整场/战利品）
│       ├── runtime-session.ts          # runtime preview 构建 + 主状态投影
│       ├── worldbook.ts                # SillyTavern 世界书读取、条目导入、运行时序列化
│       ├── battle-updates.ts           # MVU 更新路径校验 + 合并 + 应用
│       ├── field-selection.ts          # 字段树构建 + 字段勾选/排除逻辑
│       ├── field-analysis.ts           # AI 字段分析（让 AI 推荐需要发送的字段）
│       ├── frontend-settings.ts        # 战斗设置持久化（API 配置、战斗配置、提示词模板等）
│       ├── ai-profile.ts              # 全部 TypeScript 类型定义 + 默认值工厂函数
│       ├── api-client.ts              # API HTTP 客户端（axios, chat completion, 模型列表, 测试连接）
│       └── README.md                   # 现有文档
│
├── 界面/
│   └── 战斗浮窗/
│       ├── index.ts                    # Vue 挂载入口（createApp + Pinia）
│       ├── App.vue                     # ★ 完整 SPA（play 面板 + 设置页面 + 骰子弹窗 + 字段树）
│       ├── store.ts                    # Pinia store（状态同步 + 战斗操作代理 + AI 请求调度）
│       ├── FieldTreeNode.vue           # 字段树递归节点组件
│       └── global.css                  # 样式
│
├── 变量/
│   ├── initvar.yaml                    # 变量初始化
│   ├── 变量列表.txt                    # 变量列表
│   ├── 变量更新规则.yaml               # MVU 更新规则
│   └── 变量输出格式.yaml               # MVU 输出格式
│
└── 第一条消息/
    └── 0.txt                          # 第一条消息
```

---

## 一、Schema 定义（schema.ts）

### 1.1 主状态（MainState）

```typescript
MainState = {
  世界: { 当前时间, 当前日期, 当前位面, 当前地点, 当前天气, 近期事务 }  // 近期事务: Record<string, string>
  主角: { 当前化身: 单位, 旅法师: { 火花阶段, 金钱, ... } }
  队伍: Record<string, 单位>        // 每个单位有 id, 名称, 五维, HP, MP, 物/魔攻防, 技能列表, 状态效果, 装备
  敌方: Record<string, 单位>
  背包: Record<string, 物品>        // id, 名称, 数量, 标签, 描述, 效果
  任务: Record<string, 任务>        // id, 名称, 描述, 目标, 奖励, 状态
  当前可见卡: Record<string, 卡牌>
}
```

### 1.2 战斗态（BattleSession）

```typescript
BattleSession = {
  激活: boolean                      // true=战斗中
  meta: { source_message_id, mode, hero_ally_id, created_at, updated_at }
  phase: BattlePhase                 // idle | initiative | player_input | player_roll | ai_resolve | preview | finished | aborted
  round: { round_no, acting_side }   // acting_side: 无 | 玩家方 | 敌方 | 中立
  player_check: PlayerCheck          // 玩家检定
  shared_dark_pool: { values: number[], cursor }  // 共享暗骰池
  combatants: { allies, enemies }    // 当前战斗单位
  prebattle_snapshot: PrebattleSnapshot // 战前快照（开战时的完整主状态副本）
  pending_preview: PendingPreview    // AI 返回的预览草稿
  runtime: BattleRuntimeState        // 运行期状态（请看 1.3）
  round_checkpoint: RoundCheckpoint  // 回合检查点（用于回滚）
  output_mode: summary_only | full_log
}
```

### 1.3 PlayerCheck 结构

```typescript
PlayerCheck = {
  strategy_text: string     // 玩家填写的策略文本
  roll: number (0–20)       // 明骰值（初始化时为 rollD20()）
  reroll_used: number       // 已重掷次数
  confirmed: boolean        // 是否已确认（确认后锁定，进入 AI 结算）
}
```

### 1.4 Runtime 状态结构

```typescript
BattleRuntimeState = {
  last_result_type: 'none' | 'round' | 'full_battle' | 'loot'
  latest_summary: string
  latest_narration: string
  latest_battle_report: string
  latest_battle_end: boolean
  latest_battle_end_reason: string
  latest_status_changes: string[]
  latest_resource_changes: string[]
  latest_warnings: string[]
  accumulated_updates: Record<string, unknown>   // AI 返回的 incremental update 累加器
  history: Array<{
    round_no: number
    type: 'round' | 'full_battle' | 'loot'
    summary: string
    narration: string
  }>
  settlement: {
    mode: 'no_loot' | 'direct_loot' | 'checked_loot'
    mvu_commit_ready: boolean
    loot_ready: boolean
    loot_context: Record<string, unknown>
    check_prompt_needed: boolean
  }
  transcript: Array<{ id, role: 'system'|'player'|'ai', label, content, created_at }>
}
```

### 1.5 PendingPreview 结构

```typescript
PendingPreview = {
  summary: string                   // AI 输出摘要
  proposed_world_events: Record<string, string>  // 近期事务草案
  proposed_combatants: { allies, enemies }       // 下一回合 combatants 草案
  proposed_loot: Record<string, 物品>            // 战利品草案
}
```

### 1.6 RoundCheckpoint 结构

```typescript
RoundCheckpoint = {
  phase, round, player_check, shared_dark_pool,
  combatants, pending_preview, runtime   // 全量副本
}
```

---

## 二、状态机核心（session.ts）

### 2.1 生命周期

```
┌──────────────┐
│     idle     │  ← 初始态/战斗结束态
└──────┬───────┘
       │ startBattle()
       ▼
┌──────────────┐
│ player_input │  ← 玩家填写策略 + 明骰
└──────┬───────┘
       │ confirmPlayerCheck()
       ▼
┌──────────────┐
│  ai_resolve  │  ← AI 结算中（可能卡住）
└──────┬───────┘
       │ resolveConfirmedRound() → AI 返回 preview
       ▼
┌──────────────┐
│   preview    │  ← 等待玩家确认预览
└──────┬───────┘
       │ applyPendingPreview()
       ↓ player_input (下一回合)
       │ finishBattle()
       ▼
┌──────────────┐
│  finished    │  ← 战斗结束态
└──────┬───────┘
       │ commitBattle()  → 写回主状态，清空 battle_session
       │ abandonBattle() → 恢复战前快照，清空 battle_session
       ▼
┌──────────────┐
│     idle     │
└──────────────┘
```

### 2.2 关键函数

| 函数 | 位置 | 作用 | 事务边界 |
|------|------|------|----------|
| `startBattle()` | session.ts L210 | 从主状态构建 battle_session，保存快照，初始化 player_check 和暗骰池 | editCanonicalState(scope:battle_session) |
| `resumeOrRebuild()` | L222 | 检查现有 battle_session，不一致则回滚到 checkpoint | editBattleSession |
| `setStrategyText()` | L251 | 写入玩家策略 | editBattleSession |
| `rerollPlayerCheck()` | L264 | 重掷明骰（rollD20 + reroll_used+1） | editBattleSession |
| `confirmPlayerCheck()` | L323 | 锁定 player_check → ai_resolve → 调 AI 结算 | editBattleSession + resolveConfirmedRound |
| `resolveConfirmedRound()` | L292 | 调 resolve.ts → 写入 pending_preview → phase=preview | editBattleSession |
| `mockPreview()` | L340 | 本地假 preview（不调 AI） | editBattleSession |
| `applyPendingPreview()` | L353 | 应用 preview 到下一回合（combatants 更新 + 回合+1 + 阵营轮换 + 重置检定） | editBattleSession |
| `finishBattle()` | L377 | phase → finished，保留最终 checkpoint | editBattleSession |
| `commitBattle()` | L467 ➔ commit.ts | 终局写回主状态（主角化身、队伍、敌方清空、背包合并、近期事务追加） | editCanonicalState(scope:battle_session) |
| `abandonBattle()` | L469 | 恢复 prebattle_snapshot 到主状态，清空 battle_session | editCanonicalState |
| `appendRuntimeChatMessage()` | L391 | 追加 runtime transcript 条目 | editBattleSession |

### 2.3 两种流程

**经典流程（无 battle profile / no-UI 模式）：**
1. `startBattle()` → 构建 battle_session
2. `setStrategyText()` → 写入策略
3. `confirmPlayerCheck()` → 锁定 → phase=ai_resolve → resolveConfirmedRound() → 调 resolve.ts AI → 写入 preview → phase=preview
4. `applyPendingPreview()` → 应用预览 → phase=player_input（下一回合）
5. ... 循环直到 `finishBattle()` → phase=finished
6. `commitBattle()` → 写回主状态

**Runtime 流程（有 battle profile / UI 模式）：**
1. `store.executeConfiguredBattleTurn()` → 调 `runtime-ai.ts` 的 `requestBattleSingleRound()`
2. → 调 `runtime-session.ts` 的 `createPendingPreviewFromRoundResult()` 构建 preview
3. → 调 `applyRuntimeRoundPreview()` 写入 session
4. 类似地有 `executeConfiguredLootResolution()` / `applyRuntimeLootResult()`

### 2.4 事务隔离设计

- `editCanonicalState`: 读写完整 canonical state（写 scope 可选 full 或 battle_session_only）
- `editMainState`: 只读写 MainState，postCheck 确保 battle_session 不变
- `editBattleSession`: 只读写 battle_session，postCheck 确保 MainState 投影不变

这三层是强制事务隔离，不可绕过。

### 2.5 Round Checkpoint 机制

`captureRoundCheckpoint()` 在每个 player_input 起点保存全量副本。
`restoreRoundCheckpoint()` 回滚到上一个 checkpoint。
`hasUncommittedRoundState()` 检测是否有未提交的变更。

这和 step-by-step undo 类似，任何未 commit 的回合变更都可以被 `resumeOrRebuild()` 回滚。

---

## 三、骰子系统全流程分析

### 3.1 数据结构

```typescript
// session.ts
const rollD20 = () => _.random(1, 20);     // 1–20 随机
const darkPool = () => Array.from({ length: 5 }, () => rollD20());

function createFreshPlayerCheck() {
  return PlayerCheckSchema.parse({
    strategy_text: '',
    roll: 0,             // 未投骰状态；首次投骰由 rerollPlayerCheck() 写入
    reroll_used: 0,
    confirmed: false,
  });
}
```

### 3.2 服务端（session.ts）骰子操作

| 函数 | 操作 |
|------|------|
| `createFreshPlayerCheck()` | 创建新检定：roll = 0 |
| `rerollPlayerCheck()` | 投骰/重掷：roll = rollD20()；已有 roll 时 reroll_used += 1，首次投骰仍为 0；重投上限 99 |
| `confirmPlayerCheck()` | 锁定：confirmed = true |
| `buildBattleSession()` | 开战时创建 player_check + shared_dark_pool |
| `applyPendingPreview()` | 下一回合时重置：`player_check = createFreshPlayerCheck()`, `shared_dark_pool = darkPool()` |
| `applyRuntimeRoundPreview()` | 同上一行 |

### 3.3 UI 层（App.vue）骰子相关

```typescript
// reactive state
const lastDiceMessage = ref('');                                // 本地骰子消息
const lastDicePlayerCheck = ref<{roll, reroll_used, confirmed} | null>(null);  // 本地缓存
const diceDialogOpen = ref(false);
const diceAnimating = ref(false);

// computed
const effectiveDiceCheck = computed(() => {
  if (lastDicePlayerCheck.value?.roll && lastDicePlayerCheck.value.roll > 0)
    return lastDicePlayerCheck.value;                           // 优先本地
  if (battleSession.value.激活 && battleSession.value.player_check.roll > 0)
    return battleSession.value.player_check;                    // 其次 session
  return null;
});

const canOpenDiceDialog = computed(() =>
  !isFreeformRuntime && !isResolving && !isRuntimeRequestBusy
  && (!effectiveDiceCheck.value || !effectiveDiceCheck.value.confirmed)
);
const canRollDice = computed(() =>
  !isResolving && !isRuntimeRequestBusy
  && (!effectiveDiceCheck.value
    || (!effectiveDiceCheck.value.confirmed
      && (effectiveDiceCheck.value.roll <= 0 || effectiveDiceCheck.value.reroll_used < 99)))
);

// watch: 同步 session player_check 到本地
watch(() => battleSession.value.player_check, value => {
  if (battleSession.value.激活) {
    lastDicePlayerCheck.value = { roll: value.roll, reroll_used: value.reroll_used, confirmed: value.confirmed };
    return;
  }
  lastDicePlayerCheck.value = null;
}, { immediate: true });

// 本地骰子操作
const rollLocalDice = () => {
  const current = lastDicePlayerCheck.value;
  lastDicePlayerCheck.value = {
    roll: _.random(1, 20),
    reroll_used: current ? current.reroll_used + 1 : 0,
    confirmed: false,
  };
};

const reroll = async () => {
  if (battleSession.value.激活) {
    const result = await store.reroll();
    syncLastDiceCheckFromResult(result);
  } else {
    rollLocalDice();
  }
  showDiceDialog(true);
};

const acceptDiceDialog = async () => {
  if ((effectiveDiceCheck.value?.roll ?? 0) <= 0) {
    if (battleSession.value.激活) {
      const result = await store.reroll();
      syncLastDiceCheckFromResult(result);
    } else {
      rollLocalDice();
    }
  }
  lastDiceMessage.value = `技能：1d20=${diceRollLabel.value}`;
  diceDialogOpen.value = false;
  if (battleSession.value.激活) {
    store.appendRuntimeChatMessage({ role: 'system', label: '骰子', content: lastDiceMessage.value });
  }
};

// 填充到 AI 请求的 diceInputs
const buildBattleCommandOptions = () => ({
  diceInputs: battleSession.value.激活 ? {} : {
    player_roll: effectiveDiceCheck.value?.roll || undefined,   // 取本地值
    reroll_used: effectiveDiceCheck.value?.reroll_used ?? 0,
    dark_pool_remaining: Array.from({ length: 5 }, () => _.random(1, 20)), // 仅无 session 测试请求使用
    dark_pool_cursor: 0,
  }
});
```

### 3.4 store.ts 骰子操作

```typescript
// store 直接用 session controller
const reroll = () => runAndRefresh(() => battleSessionController.rerollPlayerCheck(sourceMessageId.value));
const saveStrategy = (text: string) => runAndRefresh(() => battleSessionController.setStrategyText(sourceMessageId.value, text));
```

### 3.5 骰子数据流总结

```
createFreshPlayerCheck()          UI                     AI Request
        │                          │                        │
        │ roll = 0                  │                        │
        │ reroll_used = 0           │                        │
        │ confirmed = false         │                        │
        ▼                          │                        │
  battle_session.player_check ─────┤                        │
        │                          │ roll=0                 │ → 骰子按键可用
        │                          │                        │
        │   watch(player_check) ───┤                        │
        │   → lastDicePlayerCheck  │                        │
        │                          │                        │
        │  【用户点骰子】           │                        │
        │   openDiceDialog()       │                        │
        │   syncLastDiceCheck()    │                        │
        │   → 展示当前 roll 或 --  │                        │
        │                          │                        │
        │  【用户点重掷】           │                        │
        │   reroll()               │                        │
        │   → store.reroll()       │                        │
        │   → 写回 session.player_check│                     │
        │                          │                        │
        │  【用户点确定】           │                        │
        │   acceptDiceDialog()     │                        │
        │   → 如未投骰则先 store.reroll()│                   │
        │   → appendChatMessage()  │                        │
        │                          │                        │
        │  【用户点发送】           │                        │
        │   confirm/execute        │                        │
        │   → buildBattleCommand   │                        │
        │   → 活跃战斗不覆盖 diceInputs│                     │
        │   → store 从 session 取明骰/暗骰传给 AI│            │
```

### 3.6 已确认的 Bug 清单（已修复）

| # | 文件 | 原问题 | 修复状态 |
|---|------|------|------|
| 1 | session.ts | `createFreshPlayerCheck()` 初始化随机 roll | 已改为 roll=0，避免开战即被 UI 视为已投骰 |
| 2 | App.vue | `canOpenDiceDialog` / `canRollDice` 用“已投骰”锁死重投 | 已改为只在处理中或 confirmed 后锁定 |
| 3 | App.vue | `reroll()` 只改本地缓存 | 活跃战斗时改为调用 `store.reroll()` 写回 session |
| 4 | App.vue | `acceptDiceDialog()` 没写回 session | 未投骰确认时先调用 `store.reroll()`，再记录消息 |
| 5 | App.vue | watch(player_check) 只同步 roll>0 | 已改为活跃战斗时同步完整 player_check，包括 roll=0 |
| 6 | App.vue/store.ts | 活跃战斗 `buildBattleCommandOptions()` 用本地随机暗骰覆盖 session 暗骰 | 活跃战斗时不再传本地 diceInputs，store 使用 `createBattleSessionRuntimeOptions()` 从 session 读取明骰和暗骰 |

---

## 四、Prompt 构建（prompt.ts）

### 4.1 buildNormalPromptPayload()

```typescript
正常 prompt（非战斗）：{
  世界, 主角, 队伍, 敌方, 背包, 任务, 当前可见卡,
  prompt_guard: { excludes: ['battle_session', 'CombatLog', 'DicePool'] }
}
```

### 4.2 buildBattleRoundPrompt(session)

战斗 prompt，顺序固定为：
1. **战斗规则摘要**（硬编码 4 条：阵营回合制、每单位 1 主技能+1 动作、AI 代控队友、明骰消耗等）
2. **原始 MVU 数据**（从 `prebattle_snapshot` + 当前 `combatants` 重构，让 AI 自己比对变化）
3. **历史回合**（`runtime.history` 最近最多 20 条）
4. **本回合上下文**（meta, round, phase, 玩家策略, 玩家明骰, 暗骰池剩余, output_mode）

### 4.3 createPendingPreviewFromPrompt()

仅用于 `mockPreview()`（不调 AI 的本地调试模式），构造一个带摘要、combatants、world_events 的假 preview。

---

## 五、AI 结算系统

### 5.1 resolve.ts（基础版本）

```typescript
buildBattleResolveSystemPrompt() → 7 条规则
  "你是旅法师战斗回合裁定器"
  "只返回本回合预览草案"
  "必须保留原有单位 id"
  "没有明确掉落时 proposed_loot 返回空对象"
  ...

→ generateRaw({ json_schema: BATTLE_PREVIEW_JSON_SCHEMA, ordered_prompts: [system, user_with_promptPayload] })
→ 返回 PendingPreview
```

JSON Schema 强制输出：summary, proposed_world_events, proposed_combatants, proposed_loot

### 5.2 runtime-ai.ts（profile 版本）

四套独立 prompt（字段分析/单回合/整场/战利品），各自有 system/user/output contract prompt。
AI 调用的方式：
- `requestBattleSingleRound()` → `requestRuntimeJson()` → `requestBattleChatCompletion()` → axios POST /chat/completions
- 重试机制：指数退避 500ms × 2^n，可配置 retry_limit

三个 payload builder：
- `buildBattleRuntimePayload()` → `selected_data + player_command + dice_inputs + worldbook_context + environment_context + extra_instructions`
- `buildBattleLootPayload()` → 同上但 `task=resolve_loot`
- `buildBattleRuntimePromptSnapshot()` → 只构建调试快照，不发请求；正式请求仍由 `requestBattleSingleRound/FullBattle/LootResolution` 创建 prompt snapshot 后发送。

正式发送给 API 的 prompt 结构：

```typescript
messages = [
  { role: 'system', content: prompt.system_prompt },
  {
    role: 'user',
    content: [
      prompt.user_prompt,
      'runtime_payload=',
      JSON.stringify(payload, null, 2),
      prompt.output_contract_prompt,
    ].join('\n')
  }
]
```

所以判断“提示词有没有发送”时，不能只看 `lastRuntimePayload`；要看 `lastRuntimePrompt.messages` 或 `lastRuntimePrompt.final_user_message`。

三大数据验证 schema（Zod）：
- `BattleRoundResultSchema`：解析 round 结果（含 `round_index`, `summary`, `narration`, `selected_data_updates`, `status_changes`...）
- `BattleFullResultSchema`：解析整场结果（含 `rounds[]`, `final_selected_data_updates`, `battle_report`, `loot_result`, `loot_mvu_updates`, `loot_context`...）
- `BattleLootResultSchema`：解析战利品结果（含 `loot_result`, `mvu_updates`...）

状态机适配器在 `runtime-ai.ts` 内部 `normalizeBattleStateValue()` 处理中文/英文变体 → 'ongoing' | 'finished'。
`normalizeRoundDigest()` 会从摘要/叙述里的“第 N 回合”提取 round_index；提取不到时使用数组下标 fallback。这是为了解决 AI 返回整场 rounds 但每项 round_index 混乱的问题。

快速整场特殊处理：

- `requestBattleFullBattle()` 固定 `turnMode: 'full_battle'`。
- `buildBattleRuntimePayload()` 在 full_battle 时带上 `loot_protocol` 和 `settlement_mode`。
- `appendFullBattleLootContract()` 会在 prompt 上补充“同一次响应完成战利品结算”的要求。
- `settlement_mode = no_loot` 时仍要求返回空战利品结构，而不是省略字段。

### 5.3 runtime-session.ts（preview 构建）

```
createPendingPreviewFromRoundResult(session, roundResult)
  → mergeBattleRuntimeUpdates(accumulated_updates, selected_data_updates)
  → buildRuntimeMainState(session, accumulated_updates)
  → PendingPreview: { summary, proposed_world_events, proposed_combatants, proposed_loot }

createPendingPreviewFromFullBattleResult(session, fullResult)
  → 同上但用 final_selected_data_updates
  → proposed_world_events 包含全部回合的 digest

createPendingPreviewFromLootResult(session, lootResult)
  → 合并 mvu_updates + loot_result 到 preview
```

`projectMainStateFromBattleSession()`：把 battle_session 投影为 MainState（从 prebattle_snapshot + combatants 拼合）

`buildRuntimeMainState()`：在投影基础上施加 accumulated_updates。

---

## 六、字段树系统（field-selection.ts + field-analysis.ts）

### 6.1 字段树构建

`buildBattleFieldTree(statData)` → 递归遍历所有键值：
- scalar 显示值（最长 60 字符）
- object 显示子级数
- array 显示长度
- 中文排序

### 6.2 字段选择

- `selected_fields: BattleSelectedField[]` 持久化在 battle profile 中
- 每个 field 含 path, label, enabled, source('ai'|'manual'), reason, value_kind
- 祖先/后代关系处理：
  - `isSameOrAncestorPath()` 判断一个路径是否包含另一个
  - `hasIncludedBattleFieldAncestor()` 检测是否有已选择的祖先（用于显示"排除子级"操作）
- `extractSelectedBattleData()` 从 stat_data 中提取被选字段的数据

当前关键边界：

- Runtime 请求前，store 不信任 UI 传入的 `_selectedData` 参数，而是调用 `createSelectedRuntimeData(profile)` 重新从 `runtimeStatData` 提取。
- `runtimeStatData` 优先使用当前楼层原始 `rawMainState`，再叠加 `battle_session.runtime.accumulated_updates`。
- 这样可以保持字段路径和原始 MVU 一致，避免第三回合以后因为投影态 shape 变化导致字段选择只剩一小节。

### 6.3 AI 字段分析

`analyzeBattleFields()` → 调 AI 推荐需要观察的字段路径 → 解析为 `BattleFieldSelectionConfig`
结果写入 battle profile 的 `field_selection`，设置 `manual_review_required: true`，等待玩家确认。

### 6.4 hash 机制

- `buildBattleFieldSelectionSourceHash()`: FNV-1a hash of stat_data 结构签名 → 用于检测数据变化
- `createAnalysisInputHash()`: djb2 hash of (run_mode + battle_protocol + stat_data + ...) → 用于检测是否需要重新分析

---

## 七、世界书接入（worldbook.ts）

当前世界书不是仅停留在设置 UI，已经进入 Runtime prompt。

### 7.1 数据来源

`worldbook.ts` 通过 SillyTavern 上下文读取世界书：

- `listBattleWorldbookNames()`：通过 `/api/settings/get` 读取可选世界书名称。
- `listActiveBattleWorldbooks()`：读取当前角色绑定世界书和全局启用世界书。
- `loadBattleWorldbookContent(name, source)`：读取一本世界书的启用条目。
- `loadBattleWorldbookEntryOptions(name)`：读取具体条目列表，供 UI 多选。

### 7.2 导入与保存

`store.ts` 负责把读取到的世界书写入 battle profile：

- `importActiveWorldbooks(profile)`：自动导入角色绑定 + 全局启用世界书；会替换旧的自动来源，但保留 manual 来源。
- `importWorldbookByName(profile, name)`：手动导入整本世界书。
- `refreshWorldbookEntries(name)`：加载某本世界书的可选条目。
- `importWorldbookEntries(profile, name, entryIds)`：只导入选中的具体条目。
- `toggleImportedWorldbook()` / `removeImportedWorldbook()`：启停或删除已导入上下文。

导入结果存到：

```typescript
BattleProfile.context.imported_worldbooks: Array<{
  id, name, source: 'character' | 'global' | 'manual',
  enabled, content, entry_count, imported_at
}>
```

### 7.3 Runtime 注入

`store.createRuntimeRequestOptions()` 根据 profile 设置决定是否注入：

```typescript
worldbookContext: profile.context.include_worldbook_context
  ? serializeImportedWorldbooks(profile.context.imported_worldbooks, profile.context.worldbook_max_chars)
  : []
```

`serializeImportedWorldbooks()` 会：

- 只取 enabled 且有内容的世界书。
- 按 `worldbook_max_chars` 控制总字符预算。
- 给每段加“背景参考 - 非强制性”的 header。

因此后续如果发现世界书没进 prompt，优先检查：

1. battle profile 的 `context.include_worldbook_context` 是否开启。
2. `context.imported_worldbooks[]` 是否有 enabled 内容。
3. `worldbook_max_chars` 是否大于 0。
4. 设置页最近一次 AI 请求的 `runtime_payload.worldbook_context`。

---

## 八、MVU 更新系统（battle-updates.ts）

### 7.1 路径命名空间

AI 返回的更新路径必须前缀 `stat_data.`，否则拒绝。
路径校验阻止 `__proto__`, `prototype`, `constructor`。

```typescript
// AI 返回的示例
{ "stat_data.主角.当前化身.状态效果": [...], "stat_data.世界.近期事务.battle_1": "..." }
```

### 7.2 合并策略

- `mergeBattleRuntimeUpdates(baseUpdates, nextUpdates)`:
  - 每个回合的 selected_data_updates 累加到 accumulated_updates
  - 对象深度合并（不是直接覆盖），标量直接覆盖
- `applyBattleRuntimeUpdates(state, updates)`: 把 updates 写到 state 副本
  - 路径自动尝试 `角色数据.{path}` 作为 fallback

### 7.3 事务写入

`writeBattleFlatUpdatesToMainState()` → 用 `editMainState` 事务直接写到主状态。

---

## 九、UI 层详解（App.vue + store.ts）

### 8.1 UI 标签页

| 标签 | 内容 |
|------|------|
| `play` | 默认战斗面板：骰子 + 策略输入 + 聊天记录 + 战斗操作按钮 |
| `settings` | 设置：API 配置 / 提示词配置 / 规则配置 / 字段选择 / 运行调试 |

### 8.2 操作按钮逻辑

| 按钮 | 函数 | 触发流程 |
|------|------|----------|
| 骰子 | `openDiceDialog()` | 弹窗 → 展示当前 roll / '--' |
| 投骰 | 弹窗内按钮 | 第一次 = 生成随机 1–20 |
| 重掷 | 弹窗内 `reroll()` | 活跃战斗调用 `store.reroll()` 写回 session；无 session 的测试态只改本地缓存 |
| 确定 | `acceptDiceDialog()` | 未投骰时先投骰；记录 lastDiceMessage，追加 chat message，关闭弹窗 |
| 发送 | `confirm()` | （无 profile）→ startBattle → saveStrategy → confirm → AI resolve |
| 发送 | `confirm()` | （有 profile）→ executeConfiguredBattleTurn → AI 请求 → 写入 session |
| 应用预览 | `applyPreview()` | 把 pending_preview 应用到下一回合 |
| 结束战斗 | `finishBattle()` | phase → finished |
| 提交 | `commitBattle()` | 终局写回主状态 |
| 放弃 | `abandon()` | 恢复快照，清空 battle_session |
| 重新结算 | `resolveAgain()` | 重新调 AI |
| 假预览 | `useMockPreview()` | 不调 AI 直接造 preview |

注意：

- “刷新当前战斗数据”只重新读取当前楼层/绑定楼层变量，不是重新生成 AI 回答。
- “重新结算”/`resolveAgain()` 才是重新 roll 当前回合 AI 回答的入口。
- `closeWindow()` 走 `store.close()`，后者会尝试直接调用宿主关闭函数并 fallback 到 postMessage。

### 8.3 聊天记录构建

```typescript
chatMessages = [
  ...runtime.transcript.map(m => { id, role, label, content }),
  ...lastDiceMessage (如果是本地的),
  ...lastRuntimeRequestMessage (如果是本地的),
]
```

### 8.4 watch 链（App.vue 关键数据同步）

```
activeApiProfile           → apiProfileDraft
activeBattleProfile        → battleProfileDraft, activePromptKey (reset)
battleSession.round.round_no → lastDicePlayerCheck=null, lastDiceMessage=''  (回合切换时重置本地骰子)
battleSession.player_check.strategy_text → strategyDraft
battleSession.player_check               → lastDicePlayerCheck (同步到本地，条件: roll > 0)
battleSession.output_mode                → outputMode
battleSession.pending_preview.summary    → summaryDraft
battleSession.runtime.latest_battle_report → fullLogDraft
```

### 8.5 Store 关键状态

| state | 含义 |
|-------|------|
| `canonicalState` | 完整 canonical state（1s 轮询刷新） |
| `mainState` | 主状态投影 |
| `battleSession` | battle_session 投影 |
| `rawMainState` | 当前楼层的原始 stat_data |
| `runtimeMainState` | 投影 + accumulated_updates |
| `runtimeStatData` | rawMainState 或 runtimeMainState（取决于 battle 是否激活且有 accumulated_updates） |

---

## 十、API 客户端（api-client.ts）

- 基于 axios，支持 OpenAI 兼容 API
- `requestBattleChatCompletion()`: 自动重试（408/429/5xx + timeout + network error），指数退避
- `fetchBattleApiModels()`: GET /v1/models → 解析模型列表
- `testBattleApiConnection()`: POST ping → 验证连接
- URL 规范化：自动去除尾斜杠、处理 `/v1/chat/completions` vs `/v1` 路径
- `BattleAiParseError` 异常：携带 rawText / responseData / payload 用于调试

---

## 十一、前端设置系统（frontend-settings.ts）

持久化在 SillyTavern 脚本变量里，当前实现会同时兼容并镜像两个路径：

- `battle_frontend_settings`
- `stat_data.battle_frontend_settings`

读取时会扫描多个 script variable option，收集所有候选设置，并通过 profile 数量、active id、最新 timestamp 选择最可信的一份。保存时会写回主 scope，并同步镜像到其他可用 script scope。这个设计是为了解决“刚保存能用，但下拉菜单看不到 / 刷新页面后像没保存”的问题。

### 11.1 数据流

```
SillyTavern script variables
    → battle_frontend_settings / stat_data.battle_frontend_settings
    → battleFrontendSettingsAccess.read()
    → selectBestSettingsCandidate()
    → migrateBattleFrontendSettings()  // 统一版本迁移
    → BattleFrontendSettingsSchema.parse()
    → ref<BattleFrontendSettings>
```

### 11.2 存储结构

```typescript
BattleFrontendSettings = {
  version: 1,
  active_api_profile_id: string | null,
  active_battle_profile_id: string | null,
  api_profiles: BattleApiProfile[],
  battle_profiles: BattleProfile[],
  ui_preferences: BattleUiPreferences,
}
```

### 11.3 Profile 层级

```
BattleApiProfile (接口配置)
├── name, base_url, api_key, model, headers
├── default_request_options: { temperature, max_tokens, timeout_ms, retry_limit, ... }
└── model_discovery: { enabled, use_auth_header, response_path }

BattleProfile (战斗配置)  ──绑定──→ BattleApiProfile (via api_profile_id)
├── run_mode: dice_driven | freeform
├── default_turn_mode: round_based | full_battle
├── settlement_mode: no_loot | direct_loot | checked_loot
├── rules: { battle_protocol, loot_protocol, extra_world_rules, ... }
├── field_selection: { selected_fields[], analysis_warnings, ... }
├── prompts: { field_analysis, single_round, full_battle, loot_resolution }
├── context: { include_worldbook, include_environment, extra_context_text, ... }
├── output: { round_narration_style, show_raw_json_preview, ... }
└── debug: { save_last_ai_raw_text, allow_retry_on_invalid_json, ... }
```

### 11.4 Profile 默认值

所有 profile 类型都有 `createDefault*()` 工厂函数，Prompt 模板都有内置的默认 system/user/output contract prompt。

---

## 十二、iframe 挂载层（index.ts）

### 12.1 启动流程

```
index.yaml 注册脚本
    ↓ 用户点击 "战斗" 按钮
openWindow()
    ↓ 创建 iframe（srcdoc）
    ↓ scheduleMount() 轮询 body 就绪（最多 2s / 40 次）
mountBattleWindowApp()
    ↓ createApp + createPinia
    ↓ 挂载到 #app
```

### 12.2 通信

- `postMessage({ type: 'planeswalker:battle:open' | 'planeswalker:battle:close' })`
- 直接属性访问 `window.__planeswalkerBattleWindowOpen` / `Close`
- 关闭时通知所有 frame（parent, top, 所有同辈 frame）

### 12.3 错误处理

启动失败 → 显示错误页（重试/关闭按钮）+ toastr.error

---

## 十三、MVU 状态访问层（state-access.ts）

### 13.1 读/写事务

```
readVariables(option) → 从 SillyTavern 读 variables
  ↓
Schema.parse(variables.stat_data) → CanonicalState
  ↓
mutate(draft) → Schema.safeParse(candidate) → postCheck
  ↓
writeVariables(updater, option) → 写回 SillyTavern
```

### 13.2 事务安全

- `editCanonicalState`: 完整读写 + Schema 校验 + postCheck
- `editMainState`: 限定只改 MainState，postCheck 保证 battle_session 不变
- `editBattleSession`: 限定只改 battle_session，postCheck 保证 MainState 投影不变
- 可选重试（maxRetries），仅 post_check_failed 时重试
- scope guard（`ensureScope()`）：检查 source_message_id 是否还指向最新楼层

### 13.3 变量 option

- `createLatestMessageVariableOption()`：`{ type: 'message', message_id: 'latest' }`
- `createMessageVariableOption(id)`：`{ type: 'message', message_id: id }`
- `resolveRuntimeLatestMessageId()`：调用 `globalThis.getCurrentMessageId()` → `getLastMessageId()`

---

## 十四、已知设计缺陷汇总

### 14.1 核心缺陷

1. **prompt.ts 的数据源不一致**：`buildNormalPromptPayload` 从 mainState 取数据，`buildBattleRoundPrompt` 从 `session.prebattle_snapshot` + `combatants` 取——两者路径/结构可能不同步
2. **经典流程 vs Runtime 流程双轨制**：`confirm()` 按有没有 `battleProfileDraft` 走完全不同的路径（一个走 session.ts 的 confirmPlayerCheck→resolve.ts，另一个走 store.ts 的 executeConfiguredBattleTurn→runtime-ai.ts）

### 14.1.1 本轮已修复

1. **骰子 UI 与 Session 不同步**：活跃战斗中的投骰/重投已改为调用 `store.reroll()`，写入 `battle_session.player_check`。
2. **活跃战斗暗骰池 UI 侧伪造**：`buildBattleCommandOptions()` 在 battle_session 激活时不再传本地 `diceInputs`，正式 runtime 请求由 store 从 `session.shared_dark_pool` 读取。无 battle_session 的测试请求仍会生成本地临时暗骰。

### 14.2 架构复杂度

- UI 层（App.vue）直接依赖了 session 层、store 层、field-selection 层、runtime-ai 层——职责边界模糊
- `lastDicePlayerCheck` 和 `lastDiceMessage` 作为本地缓存与 session 数据存在两份副本，同步规则复杂且容易出 bug
- 经典流程（无 profile）和 Runtime 流程（有 profile）对骰子的处理方式完全不同

### 14.3 代码体积

当前仍是大文件集中式实现，后续如果继续优化，应优先拆分职责，而不是继续往 `App.vue` 和 `store.ts` 堆逻辑。

| 文件 | 当前角色 | 后续拆分建议 |
|------|----------|--------------|
| `App.vue` | 完整 SPA：play/settings/骰子/字段树/调试面板 | 拆 settings section、运行调试面板、骰子弹窗 |
| `store.ts` | Pinia 状态同步、设置代理、世界书导入、AI 请求调度 | 拆 API 设置、世界书、runtime request action |
| `runtime-ai.ts` | Runtime prompt、payload、schema、AI 请求、解析修复 | 保留请求协议；可把 schema/normalize 单独拆出 |
| `frontend-settings.ts` | 设置 schema、迁移、读写和镜像持久化 | 保持单一入口，避免散落读写变量 |
| `session.ts` | 战斗状态机和事务操作 | 保持状态机集中，但可提取 runtime apply helper |
| `ai-profile.ts` | 类型定义和默认值工厂 | 保持纯数据定义 |
| `worldbook.ts` | 世界书读取、条目选择和序列化 | 保持无 UI 依赖 |
| `battle-updates.ts` | MVU 更新路径校验、合并、应用 | 保持为 runtime 写回唯一工具 |
| `runtime-session.ts` | AI result 转 pending preview / accumulated updates | 后续修解析显示优先看这里 |

### 14.4 无障碍脚本

目录下**没有**无障碍相关文件（accessibility 相关搜索结果为空）。

### 14.5 VS Code 交互

没有专门的 VS Code interaction 层。数据流是：
```
SillyTavern variables ⇄ state-access.ts (readVariables/writeVariables)
  ↕ iframe postMessage 通信 ↕
表示层（App.vue/store.ts）
```
